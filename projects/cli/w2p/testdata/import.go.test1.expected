package example

// replace: do/not/exist -> exist
import (
	one "github.com/package/do/not/replace/this/one"
	two "github.com/package/exist/two"
	"github.com/package/exist/three"
)

func example() {
    one.Print()
    two.Print()
    three.Print()
}